[package]
name = "deploy-proto"
version = "0.1.3"
edition = "2021"
description = "Generated gRPC types for deploy service"

[dependencies]
prost = "0.13"
tonic = { version = "0.12", features = ["transport"] }

[build-dependencies]
tonic-build = "0.12"
protoc-bin-vendored = "3"
