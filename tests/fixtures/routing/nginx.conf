server {
    listen 443 ssl;
    server_name test.pass.local test.block.local test.tunnel.local;
    ssl_certificate /etc/nginx/certs/cert.pem;
    ssl_certificate_key /etc/nginx/certs/key.pem;
    default_type text/plain;
    location / {
        return 200 "ROUTING_E2E_OK\n";
    }
}
