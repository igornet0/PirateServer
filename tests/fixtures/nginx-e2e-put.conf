# e2e PUT fixture: valid full config (nginx -t -c) after first deploy test
worker_processes 1;
pid /tmp/nginx-e2e.pid;
error_log /tmp/nginx-e2e-error.log;
events { worker_connections 128; }
http {
  access_log /dev/null;
  server {
    listen 18089;
    return 204 '';
  }
}
