[package]
name = "xray-export"
version = "0.1.0"
edition = "2021"
description = "Build Xray client JSON from grpc_proxy_session rows (public ingress)"

[dependencies]
deploy-db = { path = "../deploy-db" }
serde_json = "1"

[dev-dependencies]
chrono = { version = "0.4", features = ["clock"] }
