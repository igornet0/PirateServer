{
  "name": "test-project-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('test-project: no separate build; static server only')\"",
    "test": "node -e \"console.log('test-project: no unit tests in example'); process.exit(0)\"",
    "start": "node server.mjs",
    "dev": "node --watch server.mjs"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
